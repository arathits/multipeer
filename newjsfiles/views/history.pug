html
  head
    style
      include main.css
      include tab.css
      include verticaltab.css

  body
    .tab
      label.tablinks=uname
      form(style='height:auto' method='GET' action='/logout')
        input#username.form-control(type='hidden' value=uname name='username')
        button.tablinks(style='float:right') Logout
        button.tablinks(type='submit' style='float:right' formaction='/login') Home

    form#f(style='width:80%; text-align:left' class="modal-content animate")
      - var arr = [];
      - var arr2 = [];
      - arr = historyresult.split("/");
      - var pid = arr[0];
      - arr2 = arr[1].split(" ");
      label(style='padding:0px 10px 0px 0px') Id:
      label=arr2[0]
      label(style='padding:0px 10px 0px 10px') ,Current Owner:
      label=arr2[1]
      label(style='padding:0px 10px 0px 10px') ,Quantity:
      label=arr2[2]
      - for (var i = 2; i < arr.length-1; i++)
        - if(arr[i][0]==pid)
          <br/>
        - arr2 = arr[i].split(" ");
        -	if (arr[i][0]!=arr[i+1][0])
          -	if (arr[i][0]!=pid)
            label(style='padding:0px 10px 0px 10px') --->
          label(style='padding:0px 10px 0px 0px') Id:
          label=arr2[0]
          label(style='padding:0px 10px 0px 10px') ,Current Owner:
          label=arr2[1]
          label(style='padding:0px 10px 0px 10px') ,Quantity:
          label=arr2[2]
